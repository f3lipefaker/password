apiVersion: v1
kind: Service
metadata:
  name: service-template
  namespace: viptech
spec:
  selector:
    app: template
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
  type: NodePort